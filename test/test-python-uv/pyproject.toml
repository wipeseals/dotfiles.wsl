[project]
name = "test-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.3",
    "rich>=13.9.4",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
